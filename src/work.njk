---
layout: base.njk
title: Work
permalink: /work/
---

<header>
  <h1>{{ title }}</h1>
</header>

{# <h2>Case Studies</h2> #}

<ul class="posts">
  {% for post in collections.work | categoryFilter('casestudy') %}
    <li>
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <small>{{ post.data.blurb }}</small>
    </li>
  {% endfor %}
</ul>

{#
<h2>Gallery</h2>

<ul class="posts">
  {% for post in collections.work | categoryFilter('gallery') %}
    <li>
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <small>{{ post.data.blurb }}</small>
    </li>
  {% endfor %}
</ul> #}
